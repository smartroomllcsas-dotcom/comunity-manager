/**
 * Sprint 23 · Skills Registry — public types.
 *
 * Re-exports the SkillEntry interface from the auto-generated data module so
 * consumers can import from a stable path without depending on `data.generated`.
 */

export type { SkillEntry } from "./data.generated";
